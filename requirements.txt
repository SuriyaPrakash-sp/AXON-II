# Flood Prediction System — Python dependencies
# Install: pip install -r requirements.txt

# Deep learning
torch>=2.1.0
torchvision>=0.16.0        # optional, included for ecosystem consistency

# Numerical / data
numpy>=1.24.0

# Feature normalisation (optional — used in preprocess.py as an alternative)
scikit-learn>=1.3.0

# Backend
flask>=3.0.0
flask-cors>=4.0.0

# Production server (optional, use instead of flask dev server)
gunicorn>=21.2.0